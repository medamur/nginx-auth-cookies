services:
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
  server:
    build: ./server
    ports:
      - "8080:8080"
    container_name: server_test
